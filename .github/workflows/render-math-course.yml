name: Legacy semantic maths renderer
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy renderer retained for reference. Use render-semantic-algebra.yml for the current Remotion course.'
